yaourt -S htop zsh docker fzf neovim mosh

sudo mkdir -p /etc/systemd/system/docker.service.d

echo "[Service]\nExecStart=\nExecStart=/usr/bin/docker daemon -H fd:// --graph="/mnt/docker-data" --storage-driver=overlay" | sudo tee /etc/systemd/system/docker.service.d/overlay.conf

sudo systemctl daemon-reload
sudo systemctl start docker
sudo systemctl enable docker

sudo gpasswd -a cole wheel
sudo gpasswd -a cole docker

chsh -s /bin/zsh
