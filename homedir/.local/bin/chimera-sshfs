#!/usr/bin/env bash

if [[ ! -z "${SSHFS_CODE_ONLY:-}" ]]; then
	DIR="$HOME/workspace"
	REMOTE="/home/cole/workspace"
else
	DIR="$HOME/chimera_sshfs"
	REMOTE="/home/cole"
fi

mkdir -p ${DIR}
sshfs cole@chimera.mickens.io:${REMOTE} ${DIR} -C -p222

